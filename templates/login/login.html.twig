{% extends 'base.html.twig' %}

{% block title %}Login{% endblock %}

{% block body %}
    <h2>Connexion</h2>
    <form action="{{ path('app_login') }}" method="post">
        <label for="username">Email:</label>
        <input type="text" id="username" name="_username" value="{{ last_username }}">

        <label for="password">Password:</label>
        <input type="password" id="password" name="_password">

        {# If you want to control the URL the user is redirected to on success
        <input type="hidden" name="_target_path" value="/account"> #}

        <button type="submit">Connexion</button>
{% if error %}
        <div><button class="btn btn-info">{{ error.messageKey|trans(error.messageData, 'security') }}</button></div>
    {% endif %}
    </form>
    <label>Vous n'avez pas de compte ? inscrivez vous</label>
    <button><a href="{{ path('app_register') }}"><ion-icon name="folder-outline"></ion-icon></ion-icon></a></button>
    <div>
    <input type="button" class="btn btn-primary" value="Mot de passe oublié"></input>
    </div>
{% endblock %}
